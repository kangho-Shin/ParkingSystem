From aaa058657cca4db734cc56d1c31e267ca55e83d7 Mon Sep 17 00:00:00 2001
From: Codex <codex@openai.com>
Date: Sun, 20 Sep 2026 02:45:31 -0400
Subject: [PATCH] fix: align simulator image names with image server

---
 .../Parking.Contracts/VehicleImageName.cs     |  3 +-
 .../ImageServerClient.cs                      |  3 +-
 src/Tools/Parking.Simulator/EntrySimulator.cs |  6 ++--
 src/Tools/Parking.Simulator/ExitSimulator.cs  |  6 ++--
 src/Tools/Parking.Simulator/Program.cs        | 13 ++++----
 .../SimulatorEntryCommandTests.cs             |  4 +--
 tests/Parking.Domain.Tests/ContractTests.cs   |  8 +++--
 .../ImageServerClientTests.cs                 | 30 +++++++++++++++++++
 8 files changed, 56 insertions(+), 17 deletions(-)
 create mode 100644 tests/Parking.EdgeManager.Tests/ImageServerClientTests.cs

diff --git a/src/BuildingBlocks/Parking.Contracts/VehicleImageName.cs b/src/BuildingBlocks/Parking.Contracts/VehicleImageName.cs
index fa28ba1..801a9c1 100644
--- a/src/BuildingBlocks/Parking.Contracts/VehicleImageName.cs
+++ b/src/BuildingBlocks/Parking.Contracts/VehicleImageName.cs
@@ -11,6 +11,7 @@ public static class VehicleImageName
     public static string Create(
         long siteId,
         int groupnum,
+        int deviceNumber,
         long laneId,
         string eventType,
         DateTimeOffset eventDateTime,
@@ -21,7 +22,7 @@ public static class VehicleImageName
 
         return string.Create(
             CultureInfo.InvariantCulture,
-            $"{siteId:D3}_{groupnum:D3}_{laneId:D3}_{eventType}_{eventDateTime:yyyyMMddHHmmssfff}_{safeCarNumber}_{eventId:N}.jpg");
+            $"{siteId:D3}_{groupnum:D3}_{deviceNumber:D3}_{laneId:D3}_{eventType}_{eventDateTime:yyyyMMddHHmmssfff}_{safeCarNumber}_{eventId:N}.jpg");
     }
 
     public static string? FileNameOnly(string? image)
diff --git a/src/Edge/Parking.EdgeManager.Core/ImageServerClient.cs b/src/Edge/Parking.EdgeManager.Core/ImageServerClient.cs
index 5dc9307..30b86c3 100644
--- a/src/Edge/Parking.EdgeManager.Core/ImageServerClient.cs
+++ b/src/Edge/Parking.EdgeManager.Core/ImageServerClient.cs
@@ -14,8 +14,7 @@ public sealed class ImageServerClient
         Uri baseUri = new(imageServerUrl.TrimEnd('/') + "/", UriKind.Absolute);
         Uri requestUri = new(baseUri, $"api/image/download?fileName={Uri.EscapeDataString(fileName)}");
         using HttpResponseMessage response = await _httpClient.GetAsync(requestUri, cancellationToken);
-        if (response.StatusCode == System.Net.HttpStatusCode.NotFound) return null;
-        response.EnsureSuccessStatusCode();
+        if (!response.IsSuccessStatusCode) return null;
         byte[] data = await response.Content.ReadAsByteArrayAsync(cancellationToken);
         return data.Length == 0 ? null : data;
     }
diff --git a/src/Tools/Parking.Simulator/EntrySimulator.cs b/src/Tools/Parking.Simulator/EntrySimulator.cs
index 5043fcd..a1223f7 100644
--- a/src/Tools/Parking.Simulator/EntrySimulator.cs
+++ b/src/Tools/Parking.Simulator/EntrySimulator.cs
@@ -26,11 +26,13 @@ public sealed class EntrySimulator
         string carNumber,
         CancellationToken cancellationToken,
         int groupnum = 1,
-        string? inImage = null)
+        string? inImage = null,
+        int? deviceNumber = null)
     {
         DateTimeOffset inDateTime = DateTimeOffset.Now;
         inImage ??= VehicleImageName.Create(
-            siteId, groupnum, laneId, ParkingEventType.Entry,
+            siteId, groupnum, deviceNumber ?? checked((int)deviceId), laneId,
+            ParkingEventType.Entry,
             inDateTime, carNumber, eventId);
         FieldEventRequest request = new(
             eventId,
diff --git a/src/Tools/Parking.Simulator/ExitSimulator.cs b/src/Tools/Parking.Simulator/ExitSimulator.cs
index 767f7ee..4ee4a27 100644
--- a/src/Tools/Parking.Simulator/ExitSimulator.cs
+++ b/src/Tools/Parking.Simulator/ExitSimulator.cs
@@ -26,11 +26,13 @@ public sealed class ExitSimulator
         long deviceId,
         string carNumber,
         CancellationToken cancellationToken,
-        string? outImage = null)
+        string? outImage = null,
+        int? deviceNumber = null)
     {
         DateTimeOffset outDateTime = DateTimeOffset.Now;
         outImage ??= VehicleImageName.Create(
-            siteId, groupnum, laneId, ParkingEventType.Exit,
+            siteId, groupnum, deviceNumber ?? checked((int)deviceId), laneId,
+            ParkingEventType.Exit,
             outDateTime, carNumber, eventId);
         ExitEventRequest request = new(
             eventId,
diff --git a/src/Tools/Parking.Simulator/Program.cs b/src/Tools/Parking.Simulator/Program.cs
index 458449b..b0d92f3 100644
--- a/src/Tools/Parking.Simulator/Program.cs
+++ b/src/Tools/Parking.Simulator/Program.cs
@@ -22,6 +22,9 @@ if (!TryGetLong(args, "--site", out long siteId) ||
 int groupnum = TryGetLong(args, "--group", out long configuredGroupnum)
     ? checked((int)configuredGroupnum)
     : 1;
+int? deviceNumber = TryGetLong(args, "--device-number", out long configuredDeviceNumber)
+    ? checked((int)configuredDeviceNumber)
+    : null;
 
 string baseUrl = TryGetValue(args, "--url", out string configuredUrl)
     ? configuredUrl
@@ -39,10 +42,10 @@ try
     FieldEventResponse result = isEntry
         ? await new EntrySimulator(httpClient).SendAsync(
             eventId, siteId, laneId, deviceId, carNumber,
-            CancellationToken.None, groupnum)
+            CancellationToken.None, groupnum, deviceNumber: deviceNumber)
         : await new ExitSimulator(httpClient).SendAsync(
             eventId, siteId, groupnum, laneId, deviceId, carNumber,
-            CancellationToken.None);
+            CancellationToken.None, deviceNumber: deviceNumber);
     Console.WriteLine(JsonSerializer.Serialize(result));
 
     if (args.Contains("--repeat-event", StringComparer.OrdinalIgnoreCase))
@@ -50,10 +53,10 @@ try
         FieldEventResponse repeated = isEntry
             ? await new EntrySimulator(httpClient).SendAsync(
                 eventId, siteId, laneId, deviceId, carNumber,
-                CancellationToken.None, groupnum)
+                CancellationToken.None, groupnum, deviceNumber: deviceNumber)
             : await new ExitSimulator(httpClient).SendAsync(
                 eventId, siteId, groupnum, laneId, deviceId, carNumber,
-                CancellationToken.None);
+                CancellationToken.None, deviceNumber: deviceNumber);
         Console.WriteLine(JsonSerializer.Serialize(repeated));
     }
 
@@ -87,4 +90,4 @@ static bool TryGetValue(string[] values, string name, out string value)
 }
 
 static void PrintUsage() => Console.WriteLine(
-    "사용법: parking-simulator entry|exit --site 1 --group 1 --lane 10 --device 101 --car 12가3456 [--url http://localhost:5200] [--event UUID] [--repeat-event]");
+    "사용법: parking-simulator entry|exit --site 1 --group 1 --lane 10 --device 101 [--device-number 101] --car 12가3456 [--url http://localhost:5200] [--event UUID] [--repeat-event]");
diff --git a/tests/Parking.Api.Tests/SimulatorEntryCommandTests.cs b/tests/Parking.Api.Tests/SimulatorEntryCommandTests.cs
index 4161363..f8b5b4f 100644
--- a/tests/Parking.Api.Tests/SimulatorEntryCommandTests.cs
+++ b/tests/Parking.Api.Tests/SimulatorEntryCommandTests.cs
@@ -27,7 +27,7 @@ public sealed class SimulatorEntryCommandTests
         Assert.Equal("12가3456", handler.Request.CarNumber);
         Assert.Equal(ParkingEventType.Entry, handler.Request.EventType);
         Assert.EndsWith(".jpg", handler.Request.InImage);
-        Assert.StartsWith("001_001_010_Entry_", handler.Request.InImage);
+        Assert.StartsWith("001_001_101_010_Entry_", handler.Request.InImage);
     }
 
     [Fact]
@@ -43,7 +43,7 @@ public sealed class SimulatorEntryCommandTests
         Assert.Equal("/api/v1/edge/exits", handler.RequestPath);
         Assert.NotNull(handler.Request);
         Assert.Equal(ParkingEventType.Exit, handler.Request.EventType);
-        Assert.StartsWith("001_002_020_Exit_", handler.Request.OutImage);
+        Assert.StartsWith("001_002_201_020_Exit_", handler.Request.OutImage);
     }
 
     private sealed class CaptureHandler : HttpMessageHandler
diff --git a/tests/Parking.Domain.Tests/ContractTests.cs b/tests/Parking.Domain.Tests/ContractTests.cs
index 7c43cb7..fbdf843 100644
--- a/tests/Parking.Domain.Tests/ContractTests.cs
+++ b/tests/Parking.Domain.Tests/ContractTests.cs
@@ -27,7 +27,7 @@ public class ContractTests
     }
 
     [Fact]
-    public void 차량이미지명은_현장_그룹_차로를_3자리로_만든다()
+    public void 차량이미지명은_현장_그룹_장치_차로를_포함한다()
     {
         Guid eventId = Guid.Parse("4a912811-cb4d-4c3f-a70a-fe60504c3ef7");
         DateTimeOffset inDateTime = new(
@@ -36,6 +36,7 @@ public class ContractTests
         string result = VehicleImageName.Create(
             1,
             2,
+            101,
             10,
             ParkingEventType.Entry,
             inDateTime,
@@ -43,7 +44,7 @@ public class ContractTests
             eventId);
 
         Assert.Equal(
-            "001_002_010_Entry_20260919153025123_12가3456_4a912811cb4d4c3fa70afe60504c3ef7.jpg",
+            "001_002_101_010_Entry_20260919153025123_12가3456_4a912811cb4d4c3fa70afe60504c3ef7.jpg",
             result);
     }
 
@@ -54,12 +55,13 @@ public class ContractTests
             1000,
             2000,
             3000,
+            4000,
             ParkingEventType.Exit,
             new DateTimeOffset(2026, 9, 19, 15, 30, 25, TimeSpan.Zero),
             "12가/34:56",
             Guid.Empty);
 
-        Assert.StartsWith("1000_2000_3000_Exit_", result);
+        Assert.StartsWith("1000_2000_3000_4000_Exit_", result);
         Assert.Contains("_12가3456_", result);
     }
 
diff --git a/tests/Parking.EdgeManager.Tests/ImageServerClientTests.cs b/tests/Parking.EdgeManager.Tests/ImageServerClientTests.cs
new file mode 100644
index 0000000..868f9af
--- /dev/null
+++ b/tests/Parking.EdgeManager.Tests/ImageServerClientTests.cs
@@ -0,0 +1,30 @@
+using System.Net;
+using Parking.EdgeManager.Core;
+
+namespace Parking.EdgeManager.Tests;
+
+public sealed class ImageServerClientTests
+{
+    [Fact]
+    public async Task 잘못된_과거이미지명은_예외없이_빈사진으로_처리한다()
+    {
+        HttpClient httpClient = new(new ResponseHandler(HttpStatusCode.BadRequest));
+        ImageServerClient client = new(httpClient);
+
+        byte[]? result = await client.DownloadAsync(
+            "http://localhost:5400/", "invalid.jpg", CancellationToken.None);
+
+        Assert.Null(result);
+    }
+
+    private sealed class ResponseHandler : HttpMessageHandler
+    {
+        private readonly HttpStatusCode _statusCode;
+        public ResponseHandler(HttpStatusCode statusCode) => _statusCode = statusCode;
+
+        protected override Task<HttpResponseMessage> SendAsync(
+            HttpRequestMessage request,
+            CancellationToken cancellationToken) =>
+            Task.FromResult(new HttpResponseMessage(_statusCode));
+    }
+}
-- 
2.51.1

